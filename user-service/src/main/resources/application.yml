server:
  port: 8082

spring.application.name: UserServiceMS

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring-boot-ms-auth-user
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/